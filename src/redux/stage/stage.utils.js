export const setStateToSeat = (stage,seatToSet) =>{
    /*state.stage.sections.find(section=>
        section.key===action.payload.seccion).seats_by_rows.find(row=>
            row.row_name===action.payload.fila).seats.find(seat=>
                seat.id==action.payload.columna)*/
    console.log(stage);
    return stage;
}